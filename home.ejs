<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Medicompare - Your Medicine Companion</title>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600&family=Roboto:wght@400;500&display=swap"
        rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Open Sans', sans-serif;
            color: #333;
            background-color: #f8f8f8;
            line-height: 1.6;
        }

        /* Navbar */
        nav {
            background-color: #2ecc71;
            padding: 20px;
            text-align: center;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        nav ul {
            list-style: none;
            padding: 0;
        }

        nav ul li {
            display: inline;
            margin-right: 30px;
        }

        nav ul li a {
            text-decoration: none;
            color: #fff;
            font-size: 1.2em;
            font-weight: 600;
            transition: color 0.3s ease;
        }

        nav ul li a:hover {
            color: #3498db;
        }

        /* Hero Section */
        .hero {
            background: url('/static/media/homescreenbackground.jpg') no-repeat center center/cover;
            color: #fff;
            text-align: center;
            padding: 150px 20px;
            position: relative;
            overflow: hidden;
            background-attachment: fixed;
            background-position: center;
            background-size: cover;
        }

        .hero h1 {
            font-size: 4em;
            font-weight: 700;
            text-shadow: 3px 3px 8px rgba(0, 0, 0, 0.4);
            margin-bottom: 20px;
            animation: slideInFromTop 1s ease-in-out;
        }

        .hero p {
            font-size: 1.5em;
            margin-bottom: 40px;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
            animation: slideInFromBottom 1s ease-in-out;
        }

        .cta-button {
            background-color: #3498db;
            color: #fff;
            padding: 15px 30px;
            text-decoration: none;
            font-size: 1.3em;
            border-radius: 5px;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .cta-button:hover {
            background-color: #2980b9;
            transform: scale(1.1);
        }

        /* Search Bar */
        .search-bar {
            margin-top: 30px;
            padding: 10px;
            width: 60%;
            max-width: 600px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            background-color: rgba(255, 255, 255, 0.7);
            border-radius: 50px;
        }

        .search-bar input {
            width: 80%;
            padding: 10px;
            border: none;
            border-radius: 50px;
            outline: none;
            font-size: 1.1em;
        }

        .search-bar button {
            background-color: #3498db;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .search-bar button:hover {
            background-color: #2980b9;
        }

        /* Features Section */
        .features {
            display: flex;
            justify-content: space-between;
            padding: 60px 20px;
            background-color: #fff;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .features .feature-item {
            width: 30%;
            background-color: #f4f4f9;
            padding: 40px;
            border-radius: 10px;
            text-align: center;
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .features .feature-item:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .features .feature-item h3 {
            font-size: 2.2em;
            margin-bottom: 20px;
        }

        .features .feature-item p {
            font-size: 1.1em;
            color: #555;
        }

        /* Logo in Features Section */
        .features .logo {
            width: 30%;
            text-align: center;
            padding: 40px;
            background-color: #f4f4f9;
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        .features .logo img {
            max-width: 150px;
        }

        /* Product Viewing Section */
        .product-viewing {
            display: flex;
            justify-content: space-around;
            padding: 50px 20px;
            background-color: #f8f8f8;
        }

        .product-card {
            width: 200px;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            text-align: center;
            border-radius: 10px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .product-card img {
            width: 100%;
            height: 150px;
            object-fit: cover;
            border-radius: 10px;
        }

        .product-card h4 {
            margin-top: 20px;
            font-size: 1.3em;
        }

        .product-card p {
            margin: 10px 0;
            font-size: 1.1em;
            color: #777;
        }

        .product-card .price {
            font-size: 1.2em;
            color: #2ecc71;
        }

        /* Footer */
        footer {
            background-color: #2ecc71;
            color: #fff;
            padding: 30px;
            text-align: center;
            margin-top: 50px;
        }

        footer p {
            font-size: 1em;
            margin: 0;
        }

        a {
            text-decoration: none;
            color: black;
        }

        /* Media Queries */
        @media (max-width: 768px) {
            nav ul li {
                display: block;
                margin: 10px 0;
            }

            .features {
                flex-direction: column;
                align-items: center;
            }

            .features .feature-item,
            .features .logo {
                width: 80%;
                margin-bottom: 20px;
            }

            .cta-button {
                padding: 12px 25px;
            }

            .product-viewing {
                flex-direction: column;
                align-items: center;
            }

            .product-card {
                width: 80%;
                margin-bottom: 20px;
            }
        }

        /* Animations */
        @keyframes slideInFromTop {
            0% {
                transform: translateY(-50px);
                opacity: 0;
            }

            100% {
                transform: translateY(0);
                opacity: 1;
            }
        }

        #searchButton { width: fit-content; }

        @keyframes slideInFromBottom {
            0% {
                transform: translateY(50px);
                opacity: 0;
            }

            100% {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .profile {
            position: absolute;
            width: 20px;
            height: 20px;
            display: inline-block;
            margin-right: 4px;
            top: 12px;
            right: 36px;
        }
        .account-popup {
            position: absolute;
            top: 50px;
            right: 20px;
            background-color: #4adbff;
            padding: 10px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
            animation: slideInFromTop 0.3s ease;

        }
    </style>
</head>

<body>

    <!-- Navbar -->
    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="#products">Products</a></li>
            
        </ul>
        <script>
            function toggleAccountPopup() {
                const popup = document.querySelector('.account-popup');
                popup.toggleAttribute('hidden');
            }
        </script>
        <div class="profile" onclick="toggleAccountPopup()"><img src="http://www.pngall.com/wp-content/uploads/5/Profile-PNG-Clipart.png" width="50px" height="50px" alt="profile"></div>
        <div class="account-popup" hidden>
            <img src="" alt="">
            <div class="account-actions">
                <%= user != undefined ? user.name : '' %>
                <%= user != undefined ? user.email : 'Login to view Username' %>
                <hr />
                <% if (user == undefined) { %>
                <a href="/auth">Login</a>
                <% } else { %>
                <a href="/logout">Logout</a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero" id="home">

        <h1>Welcome to Medicompare</h1>
        <p>Your Trusted Companion for Health and Medicine</p>

        <!-- Search Bar for Medicines -->
        <form action="/search" method="GET">
            <div class="search-bar">
                <input type="search" id="searchMedicine" name="term" placeholder="Search for Medicine...">
                <input type="submit" value="Search" id="searchButton">
            </div>
        </form>
    </section>

    <!-- Features Section -->
    <section class="features" id="features">
        <div class="feature-item">
            <h3>Compare Medicines</h3>
            <p>Find the best medicine options, compare prices, and choose what’s right for you.</p>
        </div>

        <!-- Logo placed between Compare Medicines and Track Prescriptions -->
        <div class="logo">
            <img src="/static/media/logo1.jpg" alt="Medicompare Logo">
        </div>

        <div class="feature-item">
            <h3>Track Prescriptions</h3>
            <p>Keep track of your prescriptions and receive timely reminders to stay on top of your health.</p>
        </div>
    </section>

    <!-- Product Viewing Section -->
    <section class="product-viewing" id="products">
        <% i=1 %>
            <% med.map((med)=> { %>
                <div class="product-card">
                    <a href=<%=med.path %>>
                        <img src=<%=med.image %>>
                        <h4>
                            <%= med.name %>
                        </h4>
                        <p>
                            <%= med.manifacturer %>
                        </p>
                        <p class="price">
                            <%= med.price %> <span style="text-decoration: line-through; color: #ff8080">
                                    <%= med.discount %>
                                </span>
                        </p>
                    </a>
                </div>

                <% if (i % 3==0) { %>
    </section>
    <section class="product-viewing" id="products">
        <% } %>
            <% i++ %>
                <% }) %>
    </section>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 Medicompare. All Rights Reserved.</p>
    </footer>

</body>

</html>